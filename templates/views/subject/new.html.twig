{% extends 'common/connected.html.twig' %}

{% block navLinks %}
    <li><a href="{{ path('subject_index') }}">Back to the list</a></li>
    {{ parent() }}
{% endblock %}

{% block body %}
    <h1>Subject creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="red accent-2 col s2 offset-s1 btn waves-effect waves-light" type="submit" name="action">
        Create
    </button>
    {{ form_end(form) }}

    <ul>
        <li>
            <a class="red accent-2 col s2 offset-s1 btn waves-effect waves-light" href="{{ path('subject_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
